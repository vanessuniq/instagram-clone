// These are the routes for every single page in the application

const DASHBOARD = "/";
const LOGIN = "/login";
const SIGN_UP = "/signup";
const PROFILE = "/profile/:username";
const NOT_FOUND = "/not-found";

export { DASHBOARD, LOGIN, SIGN_UP, PROFILE, NOT_FOUND };